{{#if (is-development)}}
<AuLink
@route="mock-login"
@skin={{if @isCompact "primary" "button"}}
@icon="login"
>
  Aanmelden
</AuLink>
{{else}}
<AuLink
@route="auth.login"
@skin={{if @isCompact "primary" "button"}}
@icon="login"
>
  Aanmelden
</AuLink>
{{/if}}
