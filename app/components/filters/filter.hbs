<AuFormRow
  class={{@rowClass}}
>
  {{#if @label}}
    <AuLabel for={{@id}} class="{{if (eq @labelHidden true) 'au-u-hidden-visually'}}">
      {{@label}}
    </AuLabel>
  {{/if}}
  {{yield}}
</AuFormRow>
